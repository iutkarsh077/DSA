#include <bits/stdc++.h>
using namespace std;

int main() {
     char ch = 'A';
    char lower = tolower(ch);
    cout << "Lowercase equivalent of " << ch << " is " << lower << std::endl;
    return 0;
    return 0;
}